[gd_scene load_steps=3 format=2]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform vec2 square_size = vec2(60, 60);

void fragment(){
	//Time is a float which is in seconds
	
	// Top squares
	
	if ((int(FRAGCOORD.x) % int(square_size.x)) - (int(TIME * 10.0) % int(square_size.x)) == 0) {
		COLOR.a = 1.0;
	} else {
		COLOR.a = 0.0;
	}
	
	if ((int(FRAGCOORD.y) % int(square_size.y)) - (int(TIME * -10.0) % int(square_size.y)) == 0) {
		COLOR.a += 1.0;
	}
	
	// Bottom squares
	
	if ((int(FRAGCOORD.x - 15.0) % int(square_size.x)) - (int(TIME * -10.0) % int(square_size.x)) == 0) {
		COLOR.a += 0.5;
	}
	
	if ((int(FRAGCOORD.y + 15.0) % int(square_size.y)) - (int(TIME * 10.0) % int(square_size.y)) == 0) {
		COLOR.a += 0.5;
	}
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/square_size = Vector2( 60, 60 )

[node name="Combat" type="ColorRect"]
anchor_right = 1.0
anchor_bottom = 1.0
color = Color( 0, 0, 0, 1 )
__meta__ = {
"_edit_use_anchors_": false
}

[node name="Camera2D" type="Camera2D" parent="."]
anchor_mode = 0

[node name="ColorRect" type="ColorRect" parent="."]
material = SubResource( 2 )
anchor_right = 1.0
anchor_bottom = 1.0
color = Color( 0.54902, 0.00392157, 0.541176, 1 )
__meta__ = {
"_edit_use_anchors_": false
}
